# Fortress Python Client Dependencies
# Production-ready cryptographic and security dependencies

# Core cryptography
cryptography>=41.0.0
# Secure random number generation
secrets>=3.0.0

# Performance and async support
# Note: secrets is part of Python standard library in Python 3.6+

# Development and testing dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0

# Type checking and linting (optional)
mypy>=1.0.0
black>=23.0.0
flake8>=6.0.0

# Documentation (optional)
sphinx>=6.0.0
sphinx-rtd-theme>=1.2.0
